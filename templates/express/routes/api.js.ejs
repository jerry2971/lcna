// @flow
const express = require('express');
const winston = require('./../lib/winston');

const router = express.Router();
const TITLE = 'API_MSG';

/* GET home page. */
// (req, res, next) => {}
router.get('/', (req, res) => {
  winston.log('info', {title: TITLE, msg: 'get success'});
  res.status(200).json({msg: 'get success'});
});

module.exports = router;

